@model BlueBerry.ToysShop.Web.ViewModels.CustomerViewModel

<h1>Profile</h1>

<div>
    <h3>Customer Information</h3>
    <p>Name: @Model.FullName</p>
    <p>Email: @Model.Email</p>
    <!-- Diğer profil bilgilerini görüntüleyebilirsiniz -->
</div>

<!-- Profil bilgilerini düzenlemek için bir form -->
<h3>Edit Profile</h3>
<form asp-action="EditProfile" method="post">
    <div class="form-group">
        <label for="fullName">Full Name</label>
        <input type="text" class="form-control" id="fullName" name="FullName" value="@Model.FullName">
    </div>
    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" class="form-control" id="email" name="Email" value="@Model.Email">
    </div>
    <!-- Diğer profil bilgileri için form alanları eklenebilir -->

    <button type="submit" class="btn btn-primary">Save</button>
</form>

